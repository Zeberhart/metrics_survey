#basic_page
    =link_to "Back", root_path
    %br
    = form_for :session, :url => login_path do |f|
        = f.label :email
        = f.text_field :email
        = f.label :password
        = f.password_field :password
        = submit_tag "Submit"